import React from 'react';
import { StyleSheet, Text, View } from 'react-native';

const Scan = () => {
	return (
		<View>
			<Text>scan</Text>
		</View>
	);
};

export default Scan;

const styles = StyleSheet.create({});
